import React from "react";

export type TweetProps = {
    Id: string;
    Text: string;
    Created_at: string;
};

export const Tweet = (props: TweetProps) => {
    return (
        <>
            <div></div>
        </>
    );
};
