import React, { useEffect } from "react";

export const VocabList = () => {
    useEffect(() => {
        console.log("VocabList loaded");
    }, []);
    return <>VocabList wip...</>;
};

export default VocabList;
